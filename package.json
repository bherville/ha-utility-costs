{
  "name": "ha-utility-costs",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "version": "changeset version && node scripts/sync-version.js",
    "release": "node scripts/release.js"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1"
  }
}
